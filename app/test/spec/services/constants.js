'use strict';

describe('Service: filterConfigData', function () {

  // load the service's module
  beforeEach(module('JobminerApp'));

  // instantiate service
  var filterConfigData;
  beforeEach(inject(function (_filterConfigData_) {
    filterConfigData = _filterConfigData_;
  }));

  // it('should do something', function () {
  //   expect(!!filterConfigData).toBe(true);
  // });

});
